# Memory copy of [`CuCellArray`](@ref)s with 4x4 `SMatrix` cells

````@eval
using Markdown
Markdown.parse("""
```julia
$(read(joinpath("..", "..", "..", "examples", "memcopyCellArray3D.jl"), String))
```
""")
````
